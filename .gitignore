# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
out/
dist/
build/
.turbo/

# Testing
coverage/
*.lcov

# Environment (never commit real secrets)
.env
.env.local
.env.*.local
.env.development.local
.env.production.local

# Supabase local
supabase/.branches/
supabase/.temp/
supabase/.env

# MCP config (contains a Supabase access token — never commit)
.mcp.json

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
scratch/

# Misc
*.tsbuildinfo
.vercel/
Docs.rar
graphify-out/

# QA / Playwright artifacts
.playwright-mcp/
/tindivo-marketplace.png
/tindivo-menu.png
/tindivo-cart.png
/tindivo-admin.png
/qa-*.png

# QA credentials (test-only, never commit)
Docs/QA-CATALOGO-CREDENCIALES.md

# Playwright e2e: salida de corridas, nunca se commitea
/test-results/
/playwright-report/
/blob-report/

# sesiones de Playwright (contienen tokens locales de e2e)
e2e/.auth/
.viaje/

# Exports crudos del directorio del legacy: teléfonos, nombres y direcciones de
# personas reales. Solo se commitea `address-directory.json`, que es la salida
# ANONIMIZADA de `anonymize-address-directory.ts`.
apps/api/scripts/fixtures/*.json
!apps/api/scripts/fixtures/address-directory.json
